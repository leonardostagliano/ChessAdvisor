/// <reference types="electron-vite/node" />

/**
 * Injected by `define` in the `main` section of electron.vite.config.ts.
 * Empty unless `CHESSADVISOR_DEV_VERSION` is set when building.
 */
declare const __CHESSADVISOR_DEV_VERSION__: string
